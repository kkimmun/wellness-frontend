# Frontend 위험도 평가 템플릿

이 문서는 CLI 환경에서 구현한 Frontend 코드의 위험 요소를
구현 완료 후 검토하기 위한 템플릿이다.

프로젝트 공통 기준은 `Frontend_INFO.md`와
`컴포넌트_설계_정의서_웰니스_플랫폼_v1.3.0.pdf`를 따른다.

---

# 1. 평가 대상

이번 화면 구현에서 생성하거나 수정한 파일을 우선 검토한다.

필요한 경우 해당 코드와 직접 연결된 기존 컴포넌트와 API 코드까지 확인한다.

관련 없는 전체 프로젝트를 임의로 리팩토링하지 않는다.

---

# 2. 위험도 평가 항목

다음 항목을 간단히 확인한다.

- 화면 설계와 구현이 일치하는가
- API 명세와 호출 코드가 일치하는가
- 기존 공통 컴포넌트를 재사용할 수 있는데 중복 생성하지 않았는가
- 공통 UI와 feature 전용 UI 위치가 적절한가
- theme 값을 사용할 수 있는데 동일 값을 반복 하드코딩하지 않았는가
- GlobalStyles에 특정 화면 스타일을 작성하지 않았는가
- loading / error / empty 등의 필요한 상태 처리가 누락되지 않았는가
- 불필요한 state 중복 또는 반복 렌더링 위험이 없는가
- 이벤트 처리 또는 useEffect로 반복 요청이 발생할 가능성이 없는가
- 라우팅 또는 권한 처리에 문제가 없는가
- 화면이 과도하게 큰 컴포넌트 또는 지나치게 잘게 분리된 구조가 아닌가
- 기존 공통 컴포넌트 수정으로 다른 화면에 영향을 줄 가능성이 없는가
- 반응형 기준이 필요한 화면에서 기존 breakpoint 규칙을 벗어나지 않았는가

---

# 3. 위험도 기준

- 🟢 낮음: 현재 구현으로 사용해도 문제가 발생할 가능성이 낮음
- 🟡 중간: 구현 가능하지만 추가 검토가 필요함
- 🔴 높음: 화면 오류, 공통 UI 영향, API 오동작 또는 유지보수 문제가 발생할 가능성이 높음

---

# 4. 위험 요소별 대안

🟡 또는 🔴 위험이 발견되면 가능한 해결 방법을 2~3개 제시한다.

```text
[위험 요소]

문제:
위험도:

대안 1:
- 방식:
- 장점:
- 단점:

대안 2:
- 방식:
- 장점:
- 단점:

대안 3: (필요한 경우)
- 방식:
- 장점:
- 단점:

추천:
추천 이유:
```

현실적인 대안이 하나뿐이라면 억지로 여러 개를 만들지 않는다.

---

# 5. 평가 원칙

- 실제 변경된 코드를 직접 확인하고 평가한다.
- 컴포넌트 설계 정의서와 실제 공통 컴포넌트를 함께 확인한다.
- 모든 항목에 억지로 문제를 만들지 않는다.
- 🟢 항목은 짧게 작성한다.
- 🟡 / 🔴 항목을 중심으로 설명한다.
- 위험을 발견했다고 코드를 자동 수정하지 않는다.
- 변경이 필요한 경우 대안을 제시하고 사람이 최종 결정하도록 한다.

---

# 6. 출력 형식

## 1. 위험도 평가

| 항목                 | 위험도       | 판단 |
| -------------------- | ------------ | ---- |
| 화면 설계            | 🟢 / 🟡 / 🔴 |      |
| API 연동             | 🟢 / 🟡 / 🔴 |      |
| 컴포넌트 재사용      | 🟢 / 🟡 / 🔴 |      |
| 컴포넌트 구조        | 🟢 / 🟡 / 🔴 |      |
| 스타일 / theme       | 🟢 / 🟡 / 🔴 |      |
| 상태 관리            | 🟢 / 🟡 / 🔴 |      |
| 렌더링 / 이벤트      | 🟢 / 🟡 / 🔴 |      |
| 라우팅 / 권한        | 🟢 / 🟡 / 🔴 |      |
| 반응형               | 🟢 / 🟡 / 🔴 |      |
| 유지보수 / 영향 범위 | 🟢 / 🟡 / 🔴 |      |

## 2. 주요 위험 상세

🟡 / 🔴 항목만 작성한다.

```text
[항목]

문제:
발생 가능한 상황:
판단 이유:
```

## 3. 대안

주요 위험 요소마다 가능한 대안을 2~3개 비교하고
현재 프로젝트 기준 추천안을 제시한다.
