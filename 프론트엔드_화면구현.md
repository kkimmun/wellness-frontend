# Frontend 화면 구현 템플릿

이 문서는 CLI 환경의 AI 개발 도구를 사용하여
현재 프로젝트의 실제 코드를 확인하고 Frontend 화면 또는 화면의 일부를 구현하기 위한 템플릿이다.

프로젝트 공통 기술 스택, 디렉토리 구조, 스타일 및 컴포넌트 재사용 규칙은
`Frontend_INFO.md`를 따른다.

공통 컴포넌트와 디자인 규칙은
`컴포넌트_설계_정의서_웰니스_플랫폼_v1.3.0.pdf`를 참고한다.

---

# 1. 화면 기본 정보

## 화면명

어드민 - 명소 관리(상세조회, 수정)

## 화면 목적

- 화면 목적: 명소에 대한 관리(등록, 상세 조회 및 수정)
- 주요 사용자: 관리자
- 진입 경로: 지정된 url을 통해 진입

---

# 2. 구현 범위

전체 화면을 한 번에 구현하지 않아도 된다.

화면이 크거나 복잡한 경우
Page 전체 구조를 유지하면서 현재 요청한 Section 또는 기능만 구현한다.

---

# 3. 화면 설계 의도

## 초기 화면

1. 어드민 - 명소 등록
   (
   최초 진입시 입력창에

- 분류 :

- 일반적 특징

- 장소 설명
  템플릿 노출
  )

2. 어드민 - 명소 상세조회 3. 어드민 - 명소 수정(수정 진입 시 상세조회 API 1회 요청)

## 사용자 상호작용

1. 명소 등록
2. 명소 상세 조회
3. 기등록된 명소 수정

## 주요 화면 상태

- loading 명소 상세조회 결과를 서버에서 조회하는 동안 로딩 화면 또는 로딩 UI 표시
- success 검색 결과 정상 조회 표시
- empty 검색 결과가 없을 경우 결과 없음 메시지 표시
- error 데이터 조회 실패 시 오류 메시지 표시

---

# 4. Figma / 화면 설계

```text
https://www.figma.com/design/xMUFJ4hZ3kIJky7CgcngRH/%ED%99%94%EB%A9%B4%EC%84%A4%EA%B3%84%EC%84%9C?node-id=3994-1904&t=lXftD2sWVWyHrkq6-0
```

AI는 화면 구조와 사용자 상호작용을 이해하기 위해
제공된 Figma 또는 화면 이미지를 확인한다.

---

# 5. API 명세

현재 화면에서 사용하는 API만 작성한다.

```text
명소 추가 POST
Route Path	/admin/places/add
백엔드 URL	/api/admin/places
요청 전송 방식	multipart/form-data
요청 예시
form-data
placeName / TEXT : gimpo_title
placeDescription / TEXT : gimpo_description
addr / TEXT : addr
typeDetailNo / TEXT : 1
x_axis / TEXT : 132.xxxx
y_axis / TEXT : 37.XXX
viewCount / TEXT: 0
imageFiles / FILE : gimpo.jpg
성공 응답
{
    "code": 201,
    "data": null,
    "message": "생성 요청에 성공하였습니다."
}
실패 응답
{
"code" : 400,
"message" : "작성에 실패하였습니다..",
"data": null
}
명소 상세 조회 GET
Route Path	/admin/places/:placeNo
백엔드 URL	/admin/places/{placeNo}
요청 예시
1 *placeNo 전달 데이터
성공 응답
최초 진입 시 응답
최초 진입 시 응답
{
"code" : 200,
"message" : "조회 성공",
"data" :
	{
        "createDate": "2026/06.22",
        "placeName":"김포아울렛",
        "placeDescrpition":"김포아울렛은 멋진 장소입니다. 이유는 ...",
        "xAxis": 126.6993474182,
        "yAxis": 37.6547735922,
        "typeDetailNo": 8,
        "addr": "경기도 김포시 김포한강2로 41"
		"placeImages":[{
			        "imgOrder": 1,
              "imgPath": "https://semi-bucket-gimpo.s3.ap-northeast-2.amazonaws.com/places/",
              "originalName": "gimpo.jpg",
              "saveName": "6fb62a5f-e066-45e6-9034-eefe1ae723e4.jpg"
			}
		]
	}
}
실패 응답
{
    "code": 404,
    "message": "해당 게시글이 존재하지 않습니다.",
    "data": null
}
명소 수정 PATCH
Route Path	/admin/places/edit/:placeNo
백엔드 URL	/api/admin/places/{placeNo}
요청 예시
1 * 최초 진입 시
form-data * 수정
placeName / TEXT : gimpo_Name
placeDescription / TEXT : gimpo_description
adrr / TEXT : addr
typeDetailNo / TEXT / : 1
x_axis / TEXT : 132.xxxx
y_axis / TEXT : 37.XXX
imageFiles / FILE : gimpo.jpg
성공 응답
최초 진입 시 응답
{
"code" : 200,
"message" : "조회 성공",
"data" :
	{
        "createDate": "2026/06.22",
        "placeName":"김포아울렛",
        "placeDescrpition":"김포아울렛은 멋진 장소입니다. 이유는 ...",
        "xAxis": 126.6993474182,
        "yAxis": 37.6547735922,
        "typeDetailNo": 8,
        "addr": "경기도 김포시 김포한강2로 41"
		"placeImages":[{
			        "imgOrder": 1,
              "imgPath": "https://semi-bucket-gimpo.s3.ap-northeast-2.amazonaws.com/places/",
              "originalName": "gimpo.jpg",
              "saveName": "6fb62a5f-e066-45e6-9034-eefe1ae723e4.jpg"
			}
		]
	}
}

수정 완료 시
{
    "code": 200,
    "data": null
    "message": "게시글 수정 완료"
}
실패 응답
{
"code" : 400,
"message" : "장소 수정에 실패했습니다.",
"data": null
}

장소가 없을 시
{
"code" : 404,
"message" : "수정할 장소가 존재하지 않습니다.",
"data": null
}
```

API 명세에 없는 요청이나 응답 필드를 임의로 추가하지 않는다.

---

# 6. 관련 Backend 상태값

화면에서 상태값을 사용하는 경우 실제 Backend 또는 DB에서 허용되는 값을 작성한다.

```text
상태값: del_yn
상태별 화면 처리: 삭제된 명소여도 어드민이기 때문에 노출되어야 함
상태 변경 조건: 삭제 시 y 복구 혹은 동작 시 n으로 노출
```

없으면 생략한다.

---

# 9. CLI 화면 구현 요청

현재 프로젝트의 실제 코드를 먼저 확인한 뒤
`Frontend_INFO.md`와 현재 `Frontend_화면구현.md`를 기준으로 화면을 구현한다.

## 작업 방식

1. 현재 프로젝트의 Frontend 디렉토리 구조를 확인한다.
2. 구현 대상 컴포넌트 코드를 확인한다.
3. 컴포넌트 설계 정의서 PDF를 확인한다.
4. `src/components`에서 재사용 가능한 공통 컴포넌트를 확인한다.
5. `theme.js`, `GlobalStyles.js` 및 관련 스타일 파일을 확인한다.
6. 기존 Axios 및 API 호출 구조를 확인한다.
7. 현재 요청 범위에 필요한 파일만 직접 생성하거나 수정한다.
8. 기존 코드를 다시 출력하는 대신 실제 프로젝트 파일에 변경사항을 적용한다.
9. 관련 없는 파일이나 기능은 수정하지 않는다.
10. 기존 컴포넌트와 구조를 최대한 재사용한다.
11. 구현 후 가능한 경우 build 또는 lint를 실행하여 오류를 확인한다.

기존 공통 컴포넌트, `theme.js`, `GlobalStyles.js` 등
여러 화면에 영향을 줄 수 있는 파일을 수정해야 하는 경우
수정 이유와 영향 범위를 설명하고 작업자에게 수정 여부를 확인한다.

---

# 10. 구현 기준

- 화면 설계 의도와 Figma를 기준으로 구현한다.
- API 명세의 URL, HTTP Method, Request, Response 구조를 따른다.
- 기존 프로젝트의 구조와 코드 스타일을 유지한다.
- 구현 전 컴포넌트 설계 정의서와 기존 컴포넌트를 확인한다.
- 기존 공통 컴포넌트로 구현 가능한 경우 새로 생성하지 않는다.
- 스타일 구현 전 기존 `theme.js`, `GlobalStyles.js` 및 관련 스타일 코드를 확인하고 기존 규칙을 따른다.
- 기존 Axios 설정 및 API 모듈을 우선 재사용한다.
- 요청하지 않은 기능이나 사용자 동작을 임의로 추가하지 않는다.
- 제공되지 않은 요구사항을 임의로 정책화하지 않는다.
- 화면을 불필요하게 작은 컴포넌트로 과도하게 분리하지 않는다.
- 구현 후 가능한 경우 lint 및 build를 실행하여 오류를 확인한다.
- 공통 컴포넌트나 프로젝트 공통 설정을 수정해야 하는 경우 변경 이유와 영향 범위를 설명하고 작업자에게 수정 여부를 확인한다.

---

# 12. 구현 완료 후 위험도 평가

화면 구현을 완료한 뒤
별도로 제공된 `Frontend_위험도평가.md`를 참고하여
이번에 생성하거나 수정한 코드에 대한 위험도 평가를 수행한다.

1. 화면 구현을 완료한다.
2. 변경된 파일과 코드 범위를 기준으로 검토한다.
3. `Frontend_위험도평가.md`의 기준에 따라 위험도를 평가한다.
4. 🟡 또는 🔴 위험이 발견되면 가능한 대안을 2~3개 제시한다.
5. 위험이 발견되었다는 이유만으로 구현 코드를 임의로 다시 변경하지 않는다.
6. 변경이 필요한 내용은 대안으로 제시하고 사람이 최종 결정하도록 한다.
